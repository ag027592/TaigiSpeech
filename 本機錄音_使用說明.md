# TaigiSpeech 本機錄音版（無需網路）

本版本給**現場無網路**或**老舊機器**使用。

**介面與線上版完全一致**（同一份 `index.html` / `style.css` / `script.js`），
只是底層替換掉 `MediaRecorder`，改用 Web Audio API 自寫 PCM→WAV，
所以在沒有 `MediaRecorder` 的舊 Safari 上也能錄音。

完全不依賴 Flask / ffmpeg / 任何 pip 套件 — 只要這台機器上有 Python 3 即可。

---

## 一、相容性

| 機器 | OS / 瀏覽器（推測） | 是否相容 |
|---|---|---|
| **MacBook 2011** | macOS 10.13 / Safari 11+ | ✅ shim 自動接管，老 Safari 也能錄 |
| **Windows 10 (2018)** | Edge / Chrome / Firefox | ✅ 全相容 |
| **目前這台 Windows 10** | Chrome / Edge | ✅ 全相容（已驗證） |

線上版用的瀏覽器原生 `MediaRecorder` API 在 macOS 10.13 的 Safari 11/12 並**不存在**，
本版啟動時自動載入 `local/mediarecorder-shim.js`，把 `MediaRecorder` 替換為 Web Audio
+ 自寫 WAV 編碼。錄音會優先要求最高 **96 kHz**（設備只支援 48 kHz 時就用 48 kHz），並以瀏覽器實際交出的取樣率輸出
**單聲道 / 16-bit PCM WAV**，不走 Opus 壓縮。

---

## 二、Mac 2011 上的特別注意事項

1. **首次執行**：請在 Finder 對 `start-local.command` **右鍵 → 開啟**（首次需授權執行 .command）。
   - 若雙擊出現「不能開啟」或「沒有執行權限」（從 Windows USB 拷貝過來常見）：
     開啟 Terminal，把專案資料夾拖進去切換目錄，然後執行：

     ```bash
     chmod +x start-local.command
     ./start-local.command
     ```

   - 或者直接在 Terminal 跑：

     ```bash
     cd <專案資料夾>
     python3 local_server.py
     ```

2. **Python 3 是否安裝**：開啟 Terminal 輸入：

   ```bash
   python3 --version
   ```

   - 若顯示 `Python 3.x.x`：直接可用，跳到「啟動」。
   - 若顯示「找不到指令」：
     - 到 [https://www.python.org/downloads/macos/](https://www.python.org/downloads/macos/) 下載 macOS 安裝包
     - 或執行 `xcode-select --install`
     - 若有 Homebrew：`brew install python3`

3. **瀏覽器**：Mac 內建 Safari 11+ 即可。如想要更穩定可裝 Firefox 78 ESR（macOS 10.13 上仍可裝）。
4. **麥克風權限**：第一次按「錄製」時 Safari 會跳「允許 localhost 使用麥克風？」，請點允許。

---

## 三、Windows 10 上的特別注意事項

1. **Python 是否安裝**：開命令提示字元輸入 `py -3 --version`。
   - 若沒有：到 [https://www.python.org/downloads/windows/](https://www.python.org/downloads/windows/) 下載安裝，
     **記得勾選「Add Python to PATH」**。
2. **瀏覽器**：建議用 Chrome 或 Edge（Chromium）。Firefox 也可。

---

## 四、啟動

1. 把整個 `taigispeech-record-local-bundle` 資料夾複製到目標機器的硬碟（USB 拷貝即可，**不需網路**）。
2. 雙擊：
   - **Windows**：`start-local.bat`
   - **Mac**：`start-local.command`
3. 跳出黑色終端機視窗，並會自動開啟瀏覽器到 `http://127.0.0.1:5050/`。
4. 終端機若顯示
   ```
   服務位址：http://127.0.0.1:5050/
   ```
   就成功了。**那台終端機視窗請保持開著，關掉就停止錄音服務。**

---

## 五、錄音流程（與線上版完全一樣）

老師、長者看到的就是熟悉的 5 步畫面：

1. **歡迎使用 TaigiSpeech 語音錄製工具** — 輸入全名與電話 → 下一步
2. **個人基本資料問卷**（新使用者）— 年齡、稱謂、教育程度、出生地、母語、語言流利度等
3. **選擇錄製語言** — 台語錄音 / 台語+華語
4. **選擇錄製時長**
5. **錄音主畫面** — 看影片 → 錄音 → 自動儲存

> 跟現有線上版**唯一的差異**：背景錄音引擎使用 Web Audio + WAV，
> 老 Safari 也能跑、錄音是未壓縮的單聲道 PCM WAV，最高偏好取樣率為 96 kHz，後端不需 ffmpeg。

---

## 六、錄音檔在哪裡

每位使用者一個資料夾，與線上版的目錄結構幾乎相同：

```
recordings_local/<userId>/
    user_profile.json                 ← 問卷資料
    0001_1_tw.wav                     ← 第 1 句台語
    0001_1_tw.json                    ← 該句的中繼資料
    0001_1_zh.wav                     ← 第 1 句華語
    raw/0001_1_tw.wav                 ← 同一份原始拷貝
    ...
```

WAV 規格：**瀏覽器實際交出的取樣率（最高偏好 96 kHz）/ 單聲道 / 16-bit PCM**。
同名 JSON 的 `audioInfo.raw.sampleRate` 與 `recordingInfo.trackSampleRate`
會記錄實際值；若看到 `96000`，就是這次錄音實際保存為 96 kHz；若看到 `48000`，就是實際保存為 48 kHz。

---

## 七、收回錄音檔（活動結束後）

把每台機器的 `recordings_local/` 整個資料夾複製到一個 USB / 隨身碟，
集中後再上傳到主伺服器或 Google Drive。

如果想把多台機器的錄音合併到主專案的 `recordings/` 裡：

```powershell
# Windows 主機
robocopy "USB:\machine1\recordings_local" "recordings\" /S
robocopy "USB:\machine2\recordings_local" "recordings\" /S
```

```bash
# Mac 主機
rsync -av "/Volumes/USB/machine1/recordings_local/" "recordings/"
```

---

## 八、常見問題

**Q：瀏覽器一直說「無法取得麥克風權限」？**
A：確認網址是 `http://127.0.0.1:5050/` 或 `http://localhost:5050/`，
   不是 `file://`。瀏覽器只允許 https 或 localhost 使用麥克風。

**Q：點「錄製」後完全沒聲音，停止後檔案是無聲的？**
A：通常是系統選錯麥克風。請到：
- **Mac**：「系統偏好設定 → 聲音 → 輸入」 → 選對的麥克風
- **Windows**：「設定 → 系統 → 聲音 → 輸入」 → 選對的麥克風
然後重新整理瀏覽器頁面。

**Q：終端機跳出「Address already in use」？**
A：5050 埠被佔用。改用其他埠：
- Windows：`start-local.bat --port 5060`
- Mac：在 Terminal 跑 `python3 local_server.py --port 5060`

**Q：可以同時開線上版（5001）和本機版嗎？**
A：可以。本機版用 5050，線上版用 5001，互不干擾。
   存的位置也分開（`recordings_local/` vs `recordings/`）。

**Q：MacBook 2011 的 Safari 真的能用嗎？**
A：能。本版自動載入 `mediarecorder-shim.js`，用 Web Audio API + 自寫 WAV 編碼器
   取代 Safari 11 不支援的 `MediaRecorder`，已實測通過。

**Q：本機版產生的檔案，能不能合併回線上版的 `recordings/`？**
A：可以。檔名 schema（`<index>_<intentId>_<lang>.wav` + 同名 `.json`）和
   `user_profile.json`、`raw/` 子資料夾結構，都跟線上版一致。

---

## 九、程式架構（給管理員）

```
local_server.py              ← 純 Python stdlib HTTP server（127.0.0.1:5050）
local/
  mediarecorder-shim.js      ← Web Audio + PCM→WAV 墊片（取代 MediaRecorder）
index.html                   ← 與線上版同一份
style.css                    ← 與線上版同一份
script.js                    ← 與線上版同一份，含最高 96 kHz 麥克風要求與 metadata 記錄
gemini_..._proof.json        ← 語料
videos/, posters/            ← 影片與預覽圖
start-local.bat              ← Windows 啟動器
start-local.command          ← macOS 啟動器
recordings_local/            ← 錄音檔目的地
```

`local_server.py` 在回應 `/` 時會自動把
`<script src="/local/mediarecorder-shim.js"></script>` 注入到 `index.html` 中
（**不會修改原檔**），確保 shim 在原始 `script.js` 之前載入。

跟線上版完全分離：不依賴 Flask、不依賴 ffmpeg、不會碰到 `recordings/`、
`reviews/`、`_assignments.json`，watcher 也不在乎這些檔。
